{
  "name": "zouma-village-system",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.6.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "type-check": "turbo type-check",
    "format": "prettier --check .",
    "docs:check": "node scripts/check-docs.mjs",
    "quality:gate": "node scripts/quality-gate.mjs",
    "simulation:run": "pnpm --filter @zouma/simulation cli run --cwd ../..",
    "simulation:regression": "pnpm --filter @zouma/simulation cli regression --cwd ../..",
    "simulation:compare": "pnpm --filter @zouma/simulation cli compare --cwd ../..",
    "simulation:export": "pnpm --filter @zouma/simulation cli export --cwd ../.."
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.48.1",
    "@typescript-eslint/parser": "^8.48.1",
    "eslint": "^8.57.1",
    "prettier": "^3.6.2",
    "turbo": "^2.6.1",
    "typescript": "^5.9.3"
  }
}
