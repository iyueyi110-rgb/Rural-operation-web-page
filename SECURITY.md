# 安全与使用边界

本仓库是公开可见的作品集与评审仓库。公开可见不等于授予开源许可；除平台浏览、评审和引用所必需的使用外，未经权利人书面许可，不得复制、再分发、公开镜像、商业使用或将仓库中的数据、截图和规则材料用于外部项目。

## 报告安全问题

请通过私有渠道向项目维护者报告安全问题，不要在公开 Issue、Pull Request 或演示材料中披露可利用细节。报告应包含受影响文件或组件、复现条件、影响范围和建议的临时缓解措施。

## 数据与凭证边界

- 不提交 `.env.local`、访问令牌、云密钥、真实手机号、身份证号、支付标识、精确坐标、真实订单或可识别个人的信息。
- 访谈原始记录只保存在本人控制的私有位置，不进入公开仓库。
- 运行产物、Notebook 虚拟环境、缓存、原始截图和本地日志不作为发布附件。
- `outputs/simulation/` 中的数据是模拟运营数据，不代表真实业务结果，也不能据此作出生产决策。

## 模型与 Agent 边界

- 发送给外部模型的数据必须先经过角色过滤和隐私清洗。
- AI 只生成摘要、分类和建议草稿，不直接决定退款、判责、结算或业务状态变更。
- Agent 影子模式只调用经过批准的只读工具；建议必须经过人工审批。

## 许可决策

当前仓库未附带开源许可证。未来如需开源或对外发布，必须先完成权属确认、第三方依赖审查和单独的许可证决策。
